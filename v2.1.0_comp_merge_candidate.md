# v2.1.0_comp_merge_candidate

## Executive Summary

This branch delivers production-ready cutter compensation support for Carvera Community Firmware v2.1.0 with validated on-machine behavior and a refactored architecture designed for predictable streaming execution.

Primary outcomes:
- Reliable G41/G42 compensation pipeline in G17 XY workflows.
- Arc-aware compensation for I/J-formatted G2/G3 motion.
- Unified corner and junction handling using a geometric A-B-C intersection model.
- Minimal runtime risk through bounded dual-ring buffering and explicit flush behavior.
- Integrated tool-diameter workflow support via M491.2-certified TOOL_DIA fallback when no D word is provided.

This document consolidates the design and implementation intent of the branch into a single merge narrative.

## Scope of the Pull Request

In scope:
- Cutter compensation behavior in motion processing (G40/G41/G42).
- Lookahead preprocessing and compensated output generation.
- Arc compensation stabilization and terminal arc handling.
- Corner/junction continuity and diagonal artifact elimination.
- M491.2 tooling integration required by no-D compensation workflows.
- Supporting spindle direction and probe-flow safety fixes discovered during integration.

Out of scope / intentionally limited:
- R-format arc compensation under active G41/G42.
- Mixed-plane compensation semantics (G18/G19) while compensation is active.
- Broad redesign of non-compensation motion planning subsystems.

## Entry Points Into Existing v2.1.0 Code

The branch was implemented as a bolt-on extension to existing pipeline entry points, not a replacement firmware architecture.

### Core command ingestion and dispatch
- src/modules/communication/GcodeDispatch.cpp
  - Parses and dispatches incoming G-code to subscribers via ON_GCODE_RECEIVED.

### Motion execution and compensation lifecycle
- src/modules/robot/Robot.cpp
  - Robot::on_gcode_received(): primary motion intake; chooses pass-through vs preprocessed compensated flow.
  - Robot::process_buffered_command(): executes compensated and non-compensated motion commands.
  - G40/G41/G42 handlers: lifecycle management (enable, prime, flush, disable).
- src/modules/robot/Robot.h
  - Compensation state fields and integration points exposed to Robot runtime.

### New compensation preprocessor module
- src/modules/robot/CompensationPreprocessor.h
- src/modules/robot/CompensationPreprocessor.cpp
- src/modules/robot/CompensationTypes.h
  - Provides isolated compensation geometry and buffering logic with clear API boundaries.

### Kernel-level integration points
- src/libs/Kernel.cpp
- src/libs/Kernel.h
  - Runtime wiring and module lifecycle integration for the preprocessor path.

### Tool diameter and probing integration for no-D workflows
- src/modules/tools/atc/ATCHandler.cpp
- src/modules/tools/atc/ATCHandler.h
- src/modules/tools/zprobe/ZProbe.cpp
- src/modules/tools/zprobe/ZProbe.h
- src/modules/tools/zprobe/ZProbePublicAccess.h
  - M491.2 certification marker handling, side-probe sequencing, and safety behavior needed to support TOOL_DIA fallback.

### Spindle direction support used during diameter probing
- src/modules/tools/spindle/SpindleControl.h
- src/modules/tools/spindle/SpindleControl.cpp
- src/modules/tools/spindle/PWMSpindleControl.cpp
- src/modules/tools/spindle/AnalogSpindleControl.cpp
- src/config.default
  - EXT pin direction control, inversion support, and safe no-live-reversal handling.

## What Cutter Compensation Is

Cutter compensation applies a lateral offset to the programmed toolpath so the physical cutter edge follows the intended geometry.

- G41: tool-left compensation.
- G42: tool-right compensation.
- G40: compensation cancel / return to nominal path.

Conceptually, compensation transforms programmed centerline motion into an offset trajectory by tool radius while preserving continuity at transitions.

## How This PR Accomplishes Cutter Compensation

The implementation introduces a bounded preprocessor stage between G-code intake and motion execution:

1. Intake command into Robot runtime.
2. If compensation is inactive: execute via existing path.
3. If compensation is active: feed command into compensation preprocessor.
4. Preprocessor computes compensated output moves when sufficient geometric context exists.
5. Robot executes generated compensated moves in order.
6. On G40, buffered moves are flushed deterministically before compensation disables.

This design preserves existing executor behavior and localizes compensation complexity to a dedicated module.

## Architecture: Dual Buffer Design

### Why dual buffers

Compensation needs local lookahead context. A single move cannot always be offset correctly without adjacent geometry. The design therefore maintains two synchronized ring buffers:

- Uncompensated ring: source-of-truth programmed points/moves.
- Compensated ring: generated, execution-ready offset output.

### Operational model

- The uncompensated ring collects incoming motion context.
- Once enough context exists (A-B-C), geometry is solved.
- Compensated output is produced and queued into the output ring.
- Robot drains the output ring through normal execution code.

### Determinism and load behavior

- Bounded memory footprint (fixed ring capacity).
- Deterministic flush semantics on G40.
- Streaming diagnostics maintain visibility into produced-vs-served behavior.
- The preprocessor is single-writer driven by intake flow, avoiding race-prone ownership.

## Architecture: Junction and Corner Functionality

### The A-B-C geometric model

Corner logic is solved from three points:
- A: previous point.
- B: current junction point.
- C: next point.

Cases:
- Onset case (A equals B): establish initial perpendicular offset using outgoing tangent.
- True corner (A, B, C distinct): solve intersection of incoming/outgoing offset lines to produce miter-consistent corner point.
- Terminal case (B equals C): create terminal perpendicular offset for clean exit.

### Why this matters

Earlier multi-path logic created branch-specific edge behavior and occasional diagonal artifacts at transitions. The unified A-B-C approach enforces one geometric rule-set for all junction classes, greatly reducing special-case drift and simplifying correctness reasoning.

## Arc Compensation Strategy

### Supported form

- G2/G3 with I/J center specification in G17 XY plane.

### Implementation principles

- Compute effective offset radius from uncompensated radius and tool radius with side-aware sign.
- Preserve endpoint consistency to prevent snap artifacts.
- Use incoming tangent handling for terminal arc stability.

### Guard rails

- Reject unsupported patterns (notably R-format under active compensation).
- Maintain explicit error behavior rather than silent geometric fallback.

## TOOL_DIA Integration and M491.2 Certification

This branch supports no-D compensation workflows when TOOL_DIA has been measured and certified through M491.2 process logic.

Key behavior:
- Manual TOOL_DIA writes are tracked as non-certified.
- M491.2 path stores certification marker in EEPROM reserve field.
- No-D G41/G42 only proceeds when certified diameter source is valid.

This ensures compensation diameter provenance and reduces operator risk from stale manual values.

## Probe and Spindle Safety Refinements Included In This Branch

### Side-probe dwell and retract behavior

- Second touch probing was modified to minimize sensor contact dwell.
- Calibrate-pin handling in M491.2 mode now stops motion immediately on debounced hit rather than overtraveling to a safety margin threshold.
- Sequence ordering ensures retract completes before finalize and spindle stop.

### 3D probe spring-preload learning and toolsetter interaction

- Testing established that using the same spring behavior in both the 3D tool setter and the 3D probe body makes trigger order ambiguous during probe-tool TLO measurement.
- Increasing spring force in the 3D probe body makes the tool setter trigger first and prevents the tool setter from bottoming out like the older tool length sensor.
- That mechanical change improves trigger ordering but also means the probe body itself may never trigger during TLO measurement on the new tool setter.
- To support that hardware configuration without changing default behavior for other users, the firmware now provides an optional config-gated mode:
  - zprobe.use_3dtoolsetter true
- When enabled, probe-tool TLO calibration may stop on the tool setter's calibrate signal alone during TLO calibration, instead of requiring the probe body to trigger afterward.
- Default behavior remains unchanged for machines that still rely on the original dual-trigger sequence.

### Spindle direction and runtime safety

- Added configurable spindle direction pin with inversion support.
- Prevented live spindle direction reversal while running.
- Moved direction pin setup to module load to avoid runtime config-cache access faults.

## Validation Summary

Validated through repeated on-machine and log-based checks:
- G41/G42 workflow tests and representative Fusion-style programs.
- Arc compensation coverage including circle closure and arc-line transitions.
- Repeatability checks for recursive M491.2 measurement cycles.
- Build verification for all major integration phases in this branch.

Representative observed outcomes:
- Stable compensation pipeline metrics with healthy production/serve behavior.
- No reproduced firmware hard faults attributable to the final compensation architecture in validated runs.

## Limitations and Known Constraints

- Mixed-plane compensation (G18/G19 under active comp) is not a target behavior in this branch.
- R-format compensated arcs are intentionally rejected.
- Lead-in/lead-out geometry should be explicitly programmed for robust activation/deactivation transitions.
- Machine-specific configuration remains required for spindle direction pin and probe setup.

## Minimal Essential Test Coverage For Merge

Keep tests that prove the core value claims:
- Offset direction correctness (G41/G42).
- Junction correctness (line-arc, arc-arc, full-circle continuity).
- D-word and certified no-D behavior.

A practical minimal set:
- tests/TEST_basic_cutter_comp/TEST_arc_cutter_comp/ARC_01_SINGLE_CW_G41.cnc
- tests/TEST_basic_cutter_comp/TEST_arc_cutter_comp/ARC_02_SINGLE_CCW_G42.cnc
- tests/TEST_basic_cutter_comp/TEST_arc_cutter_comp/ARC_03_FULL_CIRCLE_CLOSURE.cnc
- tests/TEST_basic_cutter_comp/TEST_arc_cutter_comp/ARC_04_LINE_ARC_LINE.cnc
- tests/TEST_basic_cutter_comp/TEST_arc_cutter_comp/ARC_05_ARC_ARC_JUNCTION.cnc
- tests/TEST_basic_cutter_comp/TEST_basic_cutter_comp/TEST_D_word_simple_square_in_square.cnc
- tests/TEST_basic_cutter_comp/TEST_fusion_post_cutter_comp/cutter comp v7 no d word.cnc

## Why This Work Is Merge-Ready

- The architecture is now coherent, bounded, and test-backed.
- Key integration risks (probe dwell, spindle direction safety, no-D diameter provenance) were resolved in this branch.
- The code changes are concentrated around explicit extension points rather than broad invasive rewrites.
- Documentation, tests, and behavior can be reduced to a clean reviewer-facing minimal set without losing technical completeness.

## Fusion 360 CAM Compensation Type Strategy

### Background: Fusion 360 Compensation Options

Fusion 360 CAM offers four compensation modes for milling toolpaths:

| Mode | G-code Output | Offset Responsibility |
|---|---|---|
| **In Computer** | No G41/G42 | All offset applied in CAM; machine runs nominal path |
| **In Control** | G41/G42 + full tool radius in D-word | Machine applies full offset from D-word |
| **Wear** | G41/G42 + difference from nominal in D-word | CAM pre-offsets path; machine applies only the measured wear delta |
| **Inverse Wear** | G41/G42 + negative difference in D-word | Inverse of above; negative D-word value |

### Current Support Status

**This branch implements "In Computer" (full offset in CAM) and "In Control" (full controller-side compensation via G41/G42) modes only.**

- **In Computer / Off:** Fully supported. CAM handles all offset geometry; the firmware receives a pre-offset toolpath and G41/G42 are not active. This is the current default and the safe fallback for all users.
- **In Control (Full Compensation):** Supported. G41/G42 activate the firmware compensation pipeline. The D-word provides the full tool radius. When a D-word is present in the gcode, it is used directly. When no D-word is present, the firmware falls back to the `TOOL_DIA` config parameter via the M491.2 measurement cycle.
- **Wear:** Not currently supported. See below.
- **Inverse Wear:** Not currently supported. No path to support exists until a tool table is available. See below.

### Fusion 360 Post Processor Guidance: Where to Place G4x and Why

This section is intended for the person editing the Fusion 360 post.
It defines exactly where `G40`, `G41`, and `G42` should be emitted in finishing toolpaths and explains the geometric reason.

#### Critical placement rules

1. Do **not** enable `G41`/`G42` during rapid positioning or during pure Z approach/retract moves.
2. Emit `G41`/`G42` immediately before the first **XY cutting lead-in** move at cutting depth.
3. Keep compensation active through the full compensated contour.
4. Emit `G40` only after a dedicated **XY lead-out** move, then perform retract/rapid moves.
5. Do not make the first compensated move an ambiguous transition (for example, compensation toggle followed immediately by non-cutting reposition logic).

#### Why this matters

The compensation pipeline is a geometric lookahead/intersection solver, not a simple per-line offset.
It computes compensated junctions from neighboring motion vectors. If compensation is toggled across very short or non-cutting transition moves, the entry/exit vectors become poorly conditioned and visible "funky" lead-in/lead-out behavior can occur even when in-path geometry is accurate.

Observed machine behavior confirms this: once `TOOL_DIA` is correct, contour body dimensions can be highly accurate while entry/exit artifacts remain if lead placement is poor.

#### Minimum lead geometry guidance for post output

- Lead-in and lead-out should be explicit XY cutting moves.
- Recommended lead length is at least the active tool radius, preferably `1.5x` to `2.0x` radius for robustness.
- For a `3.16 mm` tool (`~1.58 mm` radius), target lead lengths around `2.5 mm` to `3.5 mm`.

#### Recommended output pattern (conceptual)

```gcode
; approach and plunge without compensation
G0 X... Y...
G1 Z... F...

; enable compensation on first XY cutting lead-in
G41 D...    ; or G42 D...
G1 X... Y... F...

; compensated contour body
G1/G2/G3 ...

; dedicated XY lead-out while still compensated
G1 X... Y...
G40

; then retract/rapid
G0 Z...
G0 X... Y...
```

#### Summary for post maintainers

When editing the Fusion post, treat `G4x` emission as part of entry/exit geometry design, not just modal state toggling.
Correct placement of `G41`/`G42`/`G40` is required to preserve smooth compensated transitions at contour start/end.

### The Wear Compensation Problem: Why a Tool Table Is Required

Wear mode is operationally attractive because it produces shorter lead-in and lead-out moves — the D-word value represents only the small measured difference between the nominal programmed diameter and the actual measured diameter, rather than the full tool radius. This makes the geometry tighter and more predictable on real parts.

However, implementing Wear mode correctly requires the controller to know the **nominal (intended) tool diameter** at runtime so it can interpret the D-word delta meaningfully:

> `actual_offset = nominal_radius + D_word_value`

Without knowing the nominal diameter, the firmware cannot reconstruct the correct offset from a Wear-mode D-word alone. The D-word in Wear mode is meaningless without the baseline it refers to.

There are two paths to provide this nominal diameter to the firmware:

1. **Tool table hosted on the controller:** The controller maintains a table of tool numbers → nominal diameters. When Fusion posts a tool call (`T1 M6`), the controller looks up the nominal diameter for tool 1 and uses it as the baseline for interpreting the Wear D-word. This is the standard approach used by industrial controls (Fanuc, Haas, GRBL-HAL with tool table extensions).

2. **Read nominal diameter from gcode:** Fusion's post-processor could be configured to emit the nominal diameter as a comment or custom M-code alongside the Wear D-word. The firmware would parse it and apply it per-tool. This is a non-standard workaround and creates post-processor maintenance burden.

**Neither mechanism is currently implemented in this firmware or in the Carvera controller software.** Until one of these is available, Wear compensation cannot be enabled safely. Activating G41/G42 with a Wear-mode D-word and no nominal diameter baseline would produce incorrect offsets silently.

### The D-Word as a Simple Compensation Interface

The D-word (diameter offset register number in standard CNC; actual diameter value in this implementation) provides a straightforward path for the controller to issue compensation without requiring the CAM software to pre-bake all offset geometry into the toolpath. In this firmware:

- When a D-word is present in the gcode block activating G41/G42, it is used directly as the tool diameter for computing the offset.
- When no D-word is present, the firmware falls back to `TOOL_DIA` from `config.txt` (populated by the M491.2 certified measurement cycle).

The **compensation mode** (full vs. wear) should ultimately be a machine-level setting in `config.txt`, not determined by the CAM software alone. This allows operators to configure the machine's interpretation of incoming D-word values without requiring gcode changes.

### Petition for Tool Table Support

The community should petition for a tool table in either the firmware or the Carvera controller software. A minimal tool table would need to:

- Map tool numbers (T1, T2 … Tn) to nominal diameters.
- Be writeable from the controller UI or via M-codes (e.g., `M10 P1 R6.35` to set tool 1 nominal = 6.35 mm).
- Be readable by the compensation pipeline at G41/G42 activation time.
- Survive power cycles (stored in flash or SD card).

With a tool table in place, the path to Wear mode support becomes straightforward: at G41/G42 activation, look up the nominal diameter for the active tool number, then apply `offset = (nominal_diameter / 2) + D_word_delta`.

Until this infrastructure exists, **"In Computer" (Off) remains the safe default**, and **"In Control" (full G41/G42 with D-word or TOOL_DIA fallback) is the only controller-side compensation mode this branch supports.**

---

## Suggested Follow-On Work (Post-Merge)

- Extend formal support matrix for compensated mixed-plane transitions.
- Add optional instrumentation for trigger-to-retract timing telemetry.
- Expand automated geometry regression corpus for corner edge classes.
- Add targeted CI checks for compensation metrics and unsupported-path assertions.

## Implementation Notes Consolidated From Branch Documentation

### Mathematical corner and junction model

The finalized corner model uses a unified 5-step A-B-C algorithm:

1. Build incoming and outgoing unit direction vectors from A->B and B->C.
2. Rotate each vector by 90 degrees according to compensation side (G41 or G42) to obtain normals.
3. Build two offset lines through B using tool radius along each normal.
4. Solve line-line intersection for the miter point.
5. Fall back safely for degenerate conditions (near-parallel or zero-length segments).

This replaces the older independent-perpendicular corner approach that produced diagonal shortcuts on 90-degree transitions.

### Arc compensation policy and transition behavior

Arc support in this branch is intentionally constrained and explicit:
- Supported: G17 XY arcs using I/J center format.
- Deferred: R-format compensation and mixed-plane compensation semantics.
- Helical arcs are tolerated where XY compensation applies and Z remains programmed endpoint behavior.

Transition behavior uses explicit geometric continuity at:
- line->line,
- line->arc,
- arc->line,
- arc->arc

with endpoint/tangent stabilization to avoid terminal snap artifacts.

### Buffering architecture evolution summary

The branch converged from early exploratory buffering to a synchronized dual-ring design:
- Uncompensated ring as source-of-truth geometry.
- Compensated ring as execution-ready output.
- Single writer/driver model at intake to keep buffer advancement deterministic.
- Explicit flush semantics on G40 and controlled handoff to Robot execution path.

### 3D toolsetter integration context merged into this branch

This branch includes work needed so measured tool diameter can safely feed compensation workflows:
- M491.2 certificate marker model for TOOL_DIA provenance.
- ATC diameter probing sequence integration and finalization handling.
- Spindle direction control path for diameter measurement sequences.
- Sensor-contact dwell reduction and retract-before-stop ordering fixes.
- Optional probe-tool TLO mode for stiffer-spring 3D probe hardware paired with a non-bottoming 3D tool setter.

### External standards and behavior references

The design and behavior model align conceptually with common CNC references:
- LinuxCNC G41/G42 behavior model,
- RS274/NGC cutter compensation semantics,
- Fanuc-style G40/G41/G42 and D-word expectations.

These were used as behavioral anchors, while implementation details are adapted to Smoothieware/Carvera architecture.

## Consolidation Provenance

This single document intentionally merges and supersedes the following source documents that existed under cutter_comp_documentation:

- 3dtoolsetter_merge.md
- ARC_COMPENSATION_DESIGN.md
- BOLT_ON_ARCHITECTURE_V2.md
- CUTTER_COMPENSATION_IMPLEMENTATION.md
- DUAL_BUFFER_REFACTOR_DESIGN.md
- PHASE3_CORNER_INTERSECTION_MILESTONE.md
- Carvera Community Firmware Analysis.md
- README.md

After this merge-prep step, cutter_comp_documentation is no longer required.
